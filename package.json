{
  "name": "mind-logistics",
  "version": "1.0.0",
  "description": "mind-logistics full-stack web application",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "CI=false npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
